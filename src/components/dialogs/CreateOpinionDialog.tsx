type CreateOpinionDialogProps = {
  isOpen: boolean
	handleClose: () => void
}

export default function CreateOpinionDialog({
  isOpen,
	handleClose,
}: CreateOpinionDialogProps) {
	return <div></div>
}
